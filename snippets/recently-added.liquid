<section id="recently-added">
  <div class="recently-added-products">
    {% assign products = collections.all.products | sort: 'created_at' | reverse %}
    {% for product in products limit: 100 %}
      <div class="product" id="product-{{ forloop.index }}">
        <a href="{{ product.url }}">
          <img src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
          <div class="column-src">
            <span class="prod-info">
              <h3>{{ product.title }}</h3>
              <p>{{ product.price | money }}</p>
              {% if product.options.size > 1 %}
                <select name="id" id="product-select-{{ forloop.index }}">
                  {% for variant in product.variants %}
                    <option value="{{ variant.id }}">{{ variant.title }} - {{ variant.price | money }}</option>
                  {% endfor %}
                </select>
              {% endif %}
            </span>
            <form method="post" class="add-cart" id="add-cart-form-{{ forloop.index }}">
              <input type="hidden" name="id" value="{{ product.variants.first.id }}">
              <button type="submit" class="add-to-cart" data-product-id="{{ forloop.index }}">Add to Cart</button>
            </form>
            <button id="custom-checkout-button-{{ forloop.index }}" class="btn checkout-btns" data-variant-id="{{product.variants.first.id}}">
              <img src="//siyousen.myshopify.com/cdn/shop/files/cart.svg?v=1719585583" class="checkout-icon" alt="" />
              <span>CHECKOUT</span> <span id="total-price">{{ cart.total_price | money }}</span>
            </button>
          </div>
        </a>
      </div>
    {% endfor %}
  </div>

  
  <script>
    $(document).ready(function(){
      $(".add-cart button.add-to-cart").click(function(event){
        event.preventDefault();
        event.stopPropagation();
        
        var productId = $(this).data('product-id');
          $("#custom-checkout-button-" + productId).addClass("add-checkout");
      });
    
      $(document).click(function(){
        $(".checkout-btns").removeClass("add-checkout");
      });
    });
  </script>
  
  <script>
    let chkt = document.querySelectorAll('.add-checkout[data-variant-id]');

    chkt.forEach(button => {
      let variantId = button.getAttribute('data-variant-id');
      let data = {
        "id": variantId,
        "quantity": 1
      };
    
      jQuery.ajax({
        type: 'POST',
        url: '/cart/add.js',
        data: data,
        dataType: 'json',
        success: function() { 
          // Redirect to the checkout page
          window.location.href = '/checkout'; 
        },
        error: function() {
          // Handle errors if needed
          console.error('Error adding item to cart');
        }
      });
    });
    
  </script>
    
      

  <style>

    .product {
      position: relative;
    }
    .recently-added-products .column-src {
      position: relative;
    }
    .column-src form.add-cart #custom-checkout-button {
      position: absolute;
      content: "";
      top: 0;
      z-index:222;
    }
    #recently-added .product .checkout-btns img.checkout-icon {
      max-width: 40px;
      min-height: 40px;
      min-width: 40px;
    }
    .checkout-btns {
      background: #fd7d00;
      border: 5px solid #fca700;
      min-height: 90px;
      border-radius: 5px;
      visibility: hidden;
      flex-wrap: wrap;
      align-items: center;
      cursor: pointer;
      right: -100%;
      position: absolute;
      top: 0;
      transition:all 0.4s linear;
      -webkit-transition:all 0.4s linear;
      -moz-transition:all 0.4s linear;
      -ms-transition:all 0.4s linear;
    }
    .checkout-btns .checkout-icon {
      width: 40px;
      margin: 0;
      padding: 0;
    }
    .checkout-btns #total-price {
      color: #fbae00;
      font-size: 42px !important;
      line-height: 52px;
      font-weight: 600;
    }
    .checkout-btns span {
      font-size: 16px;
      line-height: 24px;
      font-weight: 600;
      padding: 0 10px;
      display: flex;
      flex-wrap: wrap;
      justify-content:center;
      align-content: center;
    }
    .add-checkout{
      visibility: visible !important;
      right:0 !important;
      z-index:11;
    }
    #cart-notification {
      display: none;
    }

    @media screen and (max-width:767px) {
      .checkout-btns #total-price {
        font-size: 26px !important;
        line-height: 36px;
      }
    }
  </style>

  <style>
    #recently-added {
      padding: 20px 0; 
    }
    .recently-added{
      background: #121212;
    }
    #recently-added .recently-added-products {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
    }
    #recently-added .product h3 {
        font-size: 1.5rem;
        color: #fff;
        margin: 0;
        padding: 0;
    }
    #recently-added .product p {
        font-size: 1em;
        color: #fff;
        margin: 0;
        padding: 5px 0;
    }
    #recently-added .product img {
      max-width: 100%;
      width: 100%;
      height: auto;
      min-height: 440px;
      object-fit: cover;
      min-width: 255px;
      max-height: 440px;
    }
    #recently-added .recently-added-products .product {
      max-width: 100%;
      margin-bottom: 2rem;
      width: 49%;
    }
    #recently-added .recently-added-products .product a {
      text-decoration: none;
    }
    .recently-added-products {
      padding-bottom: 10rem;
    }
    .recently-added-products .add-cart button {
      background: green;
      border-radius: 5px;
      border: 2px solid #4b914b;
      min-height: 50px;
      width: 100%;
      color: #fff;
      font-size: 16px;
      line-height: 20px;
      cursor: pointer;
      text-transform: uppercase;
      max-width: 70%;
      float: right;
    }
    .recently-added-products .column-src {
      display: flex;
      flex-wrap: wrap;
      align-content: center;
    }
    .recently-added-products .column-src .prod-info,.recently-added-products .column-src .add-cart {
      width: 50%;
    }
    .recently-added-products .add-cart button:hover, .product-form__buttons button.product-form__submit:hover {
      background: #0e6f0bbf;
      border-color: #4b914b;
    }
    
    @media screen and (max-width:1600px) {
      #recently-added .product img {
        min-width: inherit;
      }
    }
    
    @media screen and (max-width:1100px) {
      #recently-added .product img {
        min-height: 180px;
      }
      #recently-added .recently-added-products .product {
        width: 48%;
      }
    }

    @media screen and (max-width:991px) {
      #recently-added .recently-added-products .product {
          max-width: 100%;
      }
      .recently-added .page-width {
          padding: 0 3rem;
      }
      #recently-added .recently-added-products .product {
        max-width: 100%;
      }
      #recently-added .product img {
        min-height: 300px;
        max-height: 300px;
      }
    }

     @media screen and (max-width:767px) {
       .recently-added .page-width {
        padding: 0 1rem;
      }
       #recently-added .recently-added-products .product {
        max-width: 100%;
      }
      .recently-added-products .add-cart button {
        min-height: 50px;
        max-width: 90%;
        font-size: 13px;
      }
    }

    @media screen and (max-width:600px) {
       #recently-added .product img {
          min-height: 220px;
          max-height:220px;
        }
        #recently-added .recently-added-products .product {
          width: 47%;
        }
    }
    
      @media screen and (max-width:480px) {
        #recently-added .recently-added-products .product {
          max-width: 100%;
          width: 100%;
        }
         #recently-added .product img {
            min-height: inherit;
            width:100%;
          }
      }

     @media screen and (max-width:370px) {
         #recently-added .recently-added-products .product {
          max-width: 100%;
        }
        #recently-added .product img {
          min-height: inherit;
          max-height:inherit;
        }
      }
    
  </style>



</section>

